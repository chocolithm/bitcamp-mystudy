<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- .properties 파일 로딩 -->
  <properties resource="config/jdbc.properties"/>

  <typeAliases>
    <!--
    <typeAlias alias="User" type="bitcamp.myapp.vo.User"/>
    <typeAlias alias="Project" type="bitcamp.myapp.vo.Project"/>
    <typeAlias alias="Board" type="bitcamp.myapp.vo.Board"/>
    -->
    <package name="bitcamp.myapp.vo"/>
  </typeAliases>

  <!-- DBMS 접속정보 -->
  <environments default="development-local">

    <!-- DBMS 접속 정보 -->
    <environment id="development-local">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
      </dataSource>
    </environment>

  </environments>

  <!-- SQL문 파일 위치 -->
  <mappers>
    <mapper resource="mappers/UserDaoMapper.xml"/>
    <mapper resource="mappers/BoardDaoMapper.xml"/>
    <mapper resource="mappers/ProjectDaoMapper.xml"/>
  </mappers>
</configuration>
